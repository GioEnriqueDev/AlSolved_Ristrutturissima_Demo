import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  images: { unoptimized: true },
  basePath: '/AlSolved_Ristrutturissima_Demo',
  assetPrefix: '/AlSolved_Ristrutturissima_Demo',
};

export default nextConfig;
